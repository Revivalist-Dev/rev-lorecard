# This is an example environment file.
# Copy this file to a new file named .env and fill in the required values.
# The .env file is ignored by Git and should not be committed.

# --- General Application Settings ---

# The port the application will be accessible on from your host machine.
PORT=3000

# A secret key phrase for encrypting credentials.
# Can be any string, like a strong password.
# THIS KEY IS CRITICAL. IF YOU LOSE IT, ALL STORED CREDENTIALS WILL BE UNUSABLE.
APP_SECRET_KEY=yoursecretphrasehere

# --- VS Code Integration Settings ---
# Path on the HOST machine where temporary files for VS Code editing will be created.
# This path must be accessible by the host's VS Code CLI.
# Example for Windows: D:\vs-lorecard
VSCODE_EDIT_DEPOT_PATH=

# --- AI Provider API Keys (DEPRECATED - Use the UI to manage credentials) ---
# You can still set these for initial setup, but managing them via the UI is recommended.
OPENROUTER_API_KEY=
GOOGLE_GEMINI_KEY=
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=


# --- PostgreSQL Configuration ---
# Default credentials for the local development PostgreSQL container.
# You generally do not need to change these for local use.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=lorecard


# --- SQLite Configuration ---
# The filename for the SQLite database.
SQLITE_DB_NAME=lorecard.db