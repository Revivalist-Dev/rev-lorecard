# This is an example environment file.
# Copy this file to a new file named .env and fill in the required values.
# The .env file is ignored by Git and should not be committed.

# --- General Application Settings ---

# The port the application will be accessible on from your host machine.
PORT=3000

# --- AI Provider API Keys ---
# You must provide an API key for at least one provider you intend to use.
OPENROUTER_API_KEY=
GOOGLE_GEMINI_KEY=
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=


# --- PostgreSQL Configuration ---
# Default credentials for the local development PostgreSQL container.
# You generally do not need to change these for local use.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=lorebook_creator


# --- SQLite Configuration ---
# The filename for the SQLite database.
SQLITE_DB_NAME=lorebook_creator.db